% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/test.R
\name{test}
\alias{test}
\title{Benchmark an expression under CodSpeed}
\usage{
test(name, expr)
}
\arguments{
\item{name}{A single string naming the benchmark. Combined with the current
file path (set by \code{\link[=test_file]{test_file()}}) to form the CodSpeed benchmark URI
\verb{tests/bench/<file>::<name>}.}

\item{expr}{The expression to benchmark. Captured unevaluated and evaluated
in the calling environment.}
}
\value{
\code{NULL}, invisibly.
}
\description{
\code{test()} records the performance of \code{expr} when the R process is run under
the CodSpeed runner (\verb{codspeed run -- Rscript ...}). When not running under
CodSpeed, \code{expr} is simply evaluated once so that benchmark files remain
runnable during development.
}
\details{
The measurement strategy depends on the active CodSpeed mode (see
\code{CODSPEED_RUNNER_MODE}):
\itemize{
\item \strong{Simulation} (CPU/Callgrind): \code{expr} is evaluated exactly once inside a
tight measurement window, so the simulated instruction count reflects only
the expression.
\item \strong{Walltime} (perf): \code{expr} is evaluated many times over a short time
budget; the timing statistics are computed and written to the CodSpeed
results file (\verb{$CODSPEED_PROFILE_FOLDER/results/<pid>.json}).
}
}
